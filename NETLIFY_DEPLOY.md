# 🚀 Деплой на Netlify для вашего репозитория

Ваш репозиторий: https://github.com/AnatolyBone/-Pomoechka-MVP

## Шаг 1: Деплой фронтенда на Netlify (5 минут)

### 1.1 Подключение к Netlify

1. Зайдите на [netlify.com](https://netlify.com)
2. Нажмите **"Sign up"** и войдите через **GitHub**
3. После входа нажмите **"Add new site"** → **"Import an existing project"**
4. Выберите **GitHub** и авторизуйтесь (если нужно)
5. Найдите и выберите репозиторий: **`AnatolyBone/-Pomoechka-MVP`**

### 1.2 Настройки сборки

**Важно:** Это статический сайт, настройки минимальные:

- **Branch to deploy:** `main` (должно быть выбрано автоматически)
- **Build command:** *(оставьте пустым)*
- **Publish directory:** `.` (точка - корень проекта)

Нажмите **"Deploy site"**

### 1.3 Получите URL

После деплоя (обычно 1-2 минуты) вы получите URL типа:
```
https://pomoechka-mvp-xyz123.netlify.app
```

**Сохраните этот URL!** Он понадобится для настройки бэкенда.

## Шаг 2: Деплой бэкенда на Railway (5 минут)

### 2.1 Подключение к Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Войдите через **GitHub**
4. Выберите **"Deploy from GitHub repo"**
5. Найдите и выберите репозиторий: **`AnatolyBone/-Pomoechka-MVP`**

### 2.2 Настройки

Railway автоматически определит Node.js проект. Просто:

1. Дождитесь завершения деплоя (2-3 минуты)
2. Railway даст вам URL типа: `https://pomoechka-mvp-production.up.railway.app`
3. Нажмите на сервис → **Settings** → **Generate Domain** (если нужно)

**Сохраните этот URL!** Это URL вашего бэкенда.

## Шаг 3: Обновление конфигурации

### 3.1 Обновите `js/config.js`

Откройте файл `js/config.js` и замените:

```javascript
API_URL: 'http://localhost:3000', // ← Замените на ваш Railway URL
```

На:

```javascript
API_URL: 'https://pomoechka-mvp-production.up.railway.app', // ← Ваш Railway URL
```

### 3.2 Обновите CORS в `server.js`

Откройте `server.js` и добавьте ваш Netlify URL в массив `allowedOrigins`:

```javascript
const allowedOrigins = [
    'http://localhost:8000',
    'http://localhost:3000',
    'https://pomoechka-mvp-xyz123.netlify.app', // ← Добавьте ваш Netlify URL
];
```

### 3.3 Закоммитьте и запушьте изменения

```bash
git add js/config.js server.js
git commit -m "Configure production URLs"
git push origin main
```

После пуша:
- Netlify автоматически передеплоит фронтенд
- Railway автоматически передеплоит бэкенд

## Шаг 4: Проверка работы

1. Откройте ваш Netlify URL: `https://pomoechka-mvp-xyz123.netlify.app`
2. Откройте консоль браузера (F12)
3. Проверьте, что запросы идут на бэкенд (Railway URL)
4. Проверьте админ-панель: `https://pomoechka-mvp-xyz123.netlify.app/admin.html`

## 🎯 Итоговые URL

После деплоя у вас будет:

- **Фронтенд:** `https://pomoechka-mvp-xyz123.netlify.app`
- **Бэкенд:** `https://pomoechka-mvp-production.up.railway.app`
- **Админка:** `https://pomoechka-mvp-xyz123.netlify.app/admin.html`

## 🔧 Решение проблем

### Бэкенд не отвечает

1. Проверьте логи на Railway: **View Logs**
2. Убедитесь, что URL в `js/config.js` правильный
3. Проверьте, что бэкенд запущен (Railway показывает статус)

### CORS ошибки

1. Убедитесь, что Netlify URL добавлен в `allowedOrigins` в `server.js`
2. Перезапустите бэкенд на Railway (Settings → Restart)

### База данных

SQLite файл создается автоматически на Railway. Данные сохраняются между перезапусками.

---

## 📝 Чеклист

- [ ] Фронтенд задеплоен на Netlify
- [ ] Бэкенд задеплоен на Railway
- [ ] URL бэкенда обновлен в `js/config.js`
- [ ] Netlify URL добавлен в CORS в `server.js`
- [ ] Изменения закоммичены и запушены
- [ ] Проверена работа приложения
- [ ] Проверена админ-панель

---

**Готово!** 🎉 Ваше приложение теперь работает в продакшне!

