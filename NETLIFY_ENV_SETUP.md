# 🔐 Настройка переменных окружения на Netlify

## ✅ Правильный способ (без хардкода в коде)

### Шаг 1: Добавьте переменную окружения на Netlify

1. Зайдите в ваш проект на [Netlify](https://app.netlify.com)
2. Откройте ваш сайт
3. Перейдите в **Site settings** → **Environment variables**
4. Нажмите **"Add a variable"**
5. Добавьте:
   - **Key:** `API_URL`
   - **Value:** `https://pomoechka-backend.onrender.com` (ваш Render URL)
6. Нажмите **Save**

### Шаг 2: Как это работает

При каждом деплое Netlify:

1. Запускается команда из `netlify.toml`: `node scripts/inject-env.js`
2. Скрипт читает переменную окружения `API_URL` из Netlify
3. Заменяет плейсхолдер `{{API_URL}}` в `js/config.js` на реальное значение
4. Деплоится обновленный файл с правильным URL

### Шаг 3: Проверка

После деплоя:

1. Откройте ваш сайт на Netlify
2. Откройте консоль браузера (F12)
3. Проверьте, что запросы идут на правильный URL бэкенда

## 📋 Структура

```
Netlify Dashboard
    └── Environment Variables
            └── API_URL = https://pomoechka-backend.onrender.com
                        ↓
            netlify.toml (build command)
                        ↓
            scripts/inject-env.js (заменяет {{API_URL}})
                        ↓
            js/config.js (обновленный с реальным URL)
                        ↓
            Деплой на Netlify
```

## 🎯 Преимущества

- ✅ **URL не хранится в коде** - безопасно
- ✅ **Можно менять без изменения кода** - просто обновите переменную
- ✅ **Разные URL для разных окружений** - dev/staging/prod
- ✅ **Секреты не в репозитории** - не попадут в git

## 🔧 Локальная разработка

Для локальной разработки ничего менять не нужно:
- Используется `http://localhost:3000` по умолчанию
- Скрипт не запускается локально

## 📝 Чеклист

- [ ] Добавлена переменная `API_URL` на Netlify
- [ ] Указан правильный URL бэкенда (Render/Fly.io/Cyclic)
- [ ] Проверен деплой (скрипт должен выполниться)
- [ ] Проверена работа приложения после деплоя
- [ ] Проверено, что запросы идут на правильный URL

## 🆘 Если не работает

1. **Проверьте логи деплоя** на Netlify - должна быть строка `✅ Injected API_URL: ...`
2. **Убедитесь, что переменная добавлена** в Environment variables
3. **Проверьте имя переменной** - должно быть точно `API_URL` (регистр важен)
4. **Передеплойте** - нажмите "Trigger deploy" → "Clear cache and deploy site"

---

**Теперь URL бэкенда хранится только в настройках Netlify, а не в коде!** 🔒

