# 🔧 Исправление подключения к бэкенду

## Проблема

Приложение все еще работает как презентация (демо данные из localStorage), а не как полноценное приложение с бэкендом.

## Причина

Фронтенд не может подключиться к бэкенду, потому что:
1. Переменная окружения `API_URL` не установлена на Netlify
2. Или скрипт `inject-env.js` не отработал при деплое
3. Фронтенд использует fallback на localStorage

## ✅ Решение

### Вариант 1: Установить переменную на Netlify (правильный способ)

1. Зайдите на [Netlify](https://app.netlify.com)
2. Откройте ваш сайт
3. **Site settings** → **Environment variables**
4. Добавьте переменную:
   - **Key:** `API_URL`
   - **Value:** `https://pomoechka-mvp.onrender.com`
5. **Save**
6. **Trigger deploy** → **Clear cache and deploy site**

### Вариант 2: Временно хардкодить URL (для быстрой проверки)

Если нужно проверить прямо сейчас, можно временно указать URL в коде:

1. Откройте `js/config.js`
2. Замените строку 25:
   ```javascript
   API_URL: window.location.hostname === 'localhost' 
       ? 'http://localhost:3000' 
       : 'https://pomoechka-mvp.onrender.com', // ← Временно хардкод
   ```
3. Закоммитьте и запушьте:
   ```bash
   git add js/config.js
   git commit -m "Temporarily hardcode backend URL for testing"
   git push origin main
   ```

**После проверки верните обратно и используйте переменные окружения!**

## 🔍 Как проверить, что работает

1. Откройте ваш Netlify URL
2. Откройте консоль браузера (F12)
3. Должны увидеть:
   ```
   🔄 Initializing API...
   📡 Backend mode: https://pomoechka-mvp.onrender.com
   ✅ Backend connected
   ```
4. Если видите:
   ```
   💾 LocalStorage mode (demo)
   ```
   Значит бэкенд не подключен.

## 🎯 Что должно измениться

После подключения к бэкенду:

- ✅ Данные сохраняются в базе данных (SQLite на Render)
- ✅ Все пользователи видят одни и те же объявления
- ✅ Работает система администраторов
- ✅ Нет демо-данных - все реальное

## 📝 Чеклист

- [ ] Переменная `API_URL` установлена на Netlify
- [ ] Значение: `https://pomoechka-mvp.onrender.com`
- [ ] Сайт передеплоен после установки переменной
- [ ] В консоли браузера видно "✅ Backend connected"
- [ ] Данные сохраняются (попробуйте создать объявление)

---

**После этого приложение станет полноценным, а не презентацией!** 🚀

