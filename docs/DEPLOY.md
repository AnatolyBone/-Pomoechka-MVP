# 🚀 Деплой на Netlify через GitHub

## Шаг 1: Подготовка GitHub репозитория

### 1.1 Создайте репозиторий на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите **"New repository"**
3. Название: `pomoechka` (или любое другое)
4. Выберите **Public** или **Private**
5. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
6. Нажмите **"Create repository"**

### 1.2 Загрузите код в GitHub

```bash
# Если еще не инициализирован git
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Pomoechka MVP with backend"

# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/pomoechka.git

# Загрузите код
git branch -M main
git push -u origin main
```

## Шаг 2: Деплой фронтенда на Netlify

### 2.1 Подключение к Netlify

1. Зайдите на [netlify.com](https://netlify.com)
2. Нажмите **"Sign up"** и войдите через GitHub
3. Нажмите **"Add new site"** → **"Import an existing project"**
4. Выберите **GitHub** и авторизуйтесь
5. Выберите репозиторий `pomoechka`

### 2.2 Настройки сборки

**Важно:** Это статический сайт, настройки сборки минимальные:

- **Build command:** (оставьте пустым)
- **Publish directory:** `.` или `/` (корень проекта)
- **Base directory:** (оставьте пустым)

Нажмите **"Deploy site"**

### 2.3 Получите URL

После деплоя вы получите URL типа:
```
https://pomoechka-xyz123.netlify.app
```

## Шаг 3: Деплой бэкенда

Netlify не подходит для постоянного Node.js сервера. Используйте один из вариантов:

### Вариант A: Railway (рекомендуется) 🚂

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Нажмите **"New Project"** → **"Deploy from GitHub repo"**
4. Выберите репозиторий `pomoechka`
5. Railway автоматически определит Node.js проект
6. Добавьте переменную окружения:
   - `PORT` = `3000` (Railway сам назначит порт, но лучше указать)
7. Railway даст вам URL типа: `https://pomoechka-production.up.railway.app`

### Вариант B: Render 🌐

1. Зайдите на [render.com](https://render.com)
2. Войдите через GitHub
3. Нажмите **"New +"** → **"Web Service"**
4. Подключите репозиторий `pomoechka`
5. Настройки:
   - **Name:** `pomoechka-backend`
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** Free (или платный)
6. Нажмите **"Create Web Service"**
7. Получите URL: `https://pomoechka-backend.onrender.com`

### Вариант C: Heroku 🟣

1. Зайдите на [heroku.com](https://heroku.com)
2. Создайте аккаунт
3. Установите Heroku CLI: `npm install -g heroku`
4. Войдите: `heroku login`
5. Создайте приложение:
   ```bash
   heroku create pomoechka-backend
   git push heroku main
   ```
6. Получите URL: `https://pomoechka-backend.herokuapp.com`

## Шаг 4: Настройка переменных окружения

### На бэкенде (Railway/Render/Heroku)

Добавьте переменные окружения (если нужны в будущем):

- `NODE_ENV` = `production`
- `PORT` = `3000` (обычно назначается автоматически)

### На фронтенде (Netlify)

1. В Netlify зайдите в **Site settings** → **Environment variables**
2. Добавьте переменную:
   - **Key:** `VITE_API_URL` (или просто используйте в коде)
   - **Value:** URL вашего бэкенда (например, `https://pomoechka-production.up.railway.app`)

## Шаг 5: Обновление конфигурации фронтенда

### 5.1 Обновите `js/config.js`

Замените URL бэкенда на ваш:

```javascript
// Для разработки
API_URL: 'http://localhost:3000',

// Для продакшна (замените на ваш URL)
// API_URL: 'https://pomoechka-production.up.railway.app',
```

### 5.2 Автоматическое определение окружения

Можно сделать автоматическое определение:

```javascript
// В js/config.js
API_URL: window.location.hostname === 'localhost' 
    ? 'http://localhost:3000' 
    : 'https://pomoechka-production.up.railway.app'
```

Или используйте переменную окружения Netlify (требует сборщик типа Vite).

## Шаг 6: Настройка CORS на бэкенде

### 6.1 Обновите `server.js`

Откройте `server.js` и добавьте ваш Netlify URL в массив `allowedOrigins`:

```javascript
const allowedOrigins = [
    'http://localhost:8000',
    'http://localhost:3000',
    'https://pomoechka-xyz123.netlify.app', // ← Добавьте ваш Netlify URL
    'https://your-custom-domain.com' // ← И кастомный домен, если есть
];
```

Или используйте переменную окружения:

```javascript
// В Railway/Render/Heroku добавьте переменную:
// FRONTEND_URL = https://pomoechka-xyz123.netlify.app

const allowedOrigins = process.env.FRONTEND_URL 
    ? [process.env.FRONTEND_URL]
    : ['http://localhost:8000'];
```

### 6.2 Закоммитьте изменения

```bash
git add server.js
git commit -m "Update CORS for production"
git push
```

## Шаг 7: Настройка кастомного домена (опционально)

### На Netlify:

1. Зайдите в **Site settings** → **Domain management**
2. Нажмите **"Add custom domain"**
3. Введите ваш домен (например, `pomoechka.ru`)
4. Следуйте инструкциям по настройке DNS

### На бэкенде:

Обновите CORS, добавив ваш домен в список разрешенных.

## Шаг 8: Проверка работы

1. Откройте ваш Netlify URL
2. Откройте консоль браузера (F12)
3. Проверьте, что запросы идут на бэкенд
4. Проверьте админ-панель: `https://your-site.netlify.app/admin.html`

## 🔧 Решение проблем

### Бэкенд не отвечает

- Проверьте, что бэкенд запущен
- Проверьте логи на Railway/Render/Heroku
- Убедитесь, что URL в `config.js` правильный

### CORS ошибки

- Добавьте ваш Netlify URL в CORS настройки бэкенда
- Проверьте, что заголовки правильные

### База данных не работает

- SQLite файл должен создаваться автоматически
- На некоторых платформах (Heroku) нужна внешняя БД (PostgreSQL)
- Для продакшна лучше использовать PostgreSQL

## 📝 Структура для деплоя

```
pomoechka/
├── index.html          # → Netlify (фронтенд)
├── admin.html          # → Netlify (фронтенд)
├── css/                # → Netlify
├── js/                 # → Netlify
├── server.js           # → Railway/Render/Heroku (бэкенд)
├── package.json        # → Railway/Render/Heroku
├── db/                 # → Railway/Render/Heroku
└── routes/             # → Railway/Render/Heroku
```

## 🎯 Итоговая схема

```
GitHub Repo
    ├── Frontend → Netlify (https://pomoechka.netlify.app)
    └── Backend → Railway (https://pomoechka-api.railway.app)
```

Фронтенд на Netlify обращается к бэкенду на Railway.

---

## 💡 Полезные ссылки

- [Netlify Docs](https://docs.netlify.com/)
- [Railway Docs](https://docs.railway.app/)
- [Render Docs](https://render.com/docs)
- [Heroku Docs](https://devcenter.heroku.com/)

