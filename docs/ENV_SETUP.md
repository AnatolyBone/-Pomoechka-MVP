# 🔐 Настройка переменных окружения

## ✅ Правильный способ (без хардкода в коде)

### На Netlify (для фронтенда)

1. Зайдите в ваш проект на Netlify
2. **Site settings** → **Environment variables**
3. Добавьте переменную:
   - **Key:** `API_URL`
   - **Value:** `https://pomoechka-backend.onrender.com` (ваш Render URL)
4. Нажмите **Save**

### На Render (для бэкенда)

1. Зайдите в ваш сервис на Render
2. **Environment** → **Environment Variables**
3. Добавьте переменные (если нужны):
   - `NODE_ENV` = `production`
   - `PORT` = `3000` (обычно назначается автоматически)

## 🔄 Как это работает

1. При деплое на Netlify запускается скрипт `scripts/inject-env.js`
2. Скрипт читает переменную окружения `API_URL` из Netlify
3. Заменяет значение в `js/config.js` на значение из переменной
4. Деплоится обновленный файл

## 📝 Структура

```
Netlify Environment Variables
    └── API_URL = https://pomoechka-backend.onrender.com
            ↓
    scripts/inject-env.js (заменяет в config.js)
            ↓
    js/config.js (обновленный с правильным URL)
            ↓
    Деплой на Netlify
```

## 🎯 Преимущества

- ✅ URL бэкенда не хранится в коде
- ✅ Можно менять URL без изменения кода
- ✅ Разные URL для разных окружений (dev/staging/prod)
- ✅ Безопасность - секреты не в репозитории

## 🔧 Локальная разработка

Для локальной разработки ничего менять не нужно - используется `http://localhost:3000` по умолчанию.

## 📋 Чеклист

- [ ] Добавлена переменная `API_URL` на Netlify
- [ ] Указан правильный URL бэкенда (Render/Fly.io/Cyclic)
- [ ] Скрипт `inject-env.js` работает при деплое
- [ ] Проверена работа приложения после деплоя

---

**Теперь URL бэкенда не хранится в открытом коде!** 🔒

