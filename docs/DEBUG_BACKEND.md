# 🐛 Отладка подключения к бэкенду

## Проблема

В консоли видно:
```
📡 Backend mode: {{API_URL}}
```

Это значит, что плейсхолдер не был заменен скриптом `inject-env.js`.

## ✅ Решение

Я уже исправил `js/config.js` - теперь используется прямой URL:
```javascript
API_URL: 'https://pomoechka-mvp.onrender.com'
```

## 📝 Что нужно сделать

1. **Изменения уже закоммичены и запушены** ✅
2. **Дождитесь деплоя на Netlify** (1-2 минуты)
3. **Обновите страницу** (Ctrl+F5 для очистки кэша)
4. **Проверьте консоль** - должно быть:
   ```
   📡 Backend mode: https://pomoechka-mvp.onrender.com
   ✅ Backend connected
   ```

## 🔍 Если все еще не работает

### Проверка 1: Откройте консоль браузера (F12)

Должно быть:
- ✅ `📡 Backend mode: https://pomoechka-mvp.onrender.com`
- ✅ `✅ Backend connected`

Если видите:
- ❌ `📡 Backend mode: {{API_URL}}` - значит старая версия, обновите страницу
- ❌ `💾 LocalStorage mode (demo)` - значит бэкенд не подключен

### Проверка 2: Проверьте бэкенд напрямую

Откройте в браузере:
```
https://pomoechka-mvp.onrender.com/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "timestamp": "..."
}
```

### Проверка 3: Проверьте CORS

В консоли браузера могут быть ошибки CORS. Если есть, нужно добавить ваш Netlify URL в `server.js`:

```javascript
const allowedOrigins = [
    'http://localhost:8000',
    'https://your-netlify-url.netlify.app', // ← Добавьте ваш Netlify URL
];
```

## 🎯 После исправления

Когда все заработает, вы увидите:
- ✅ Реальные данные из базы (не демо)
- ✅ Можно создавать объявления
- ✅ Данные сохраняются между сессиями
- ✅ Работает система администраторов

---

**После деплоя обновите страницу и проверьте консоль!** 🔄

