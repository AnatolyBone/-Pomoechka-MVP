# 🚀 Настройка Render для бэкенда

## Шаг 1: Создание сервиса

1. Зайдите на [render.com](https://render.com)
2. Войдите через **GitHub**
3. Нажмите **"New +"** → **"Web Service"**
4. Подключите репозиторий: `AnatolyBone/-Pomoechka-MVP`
5. Настройки:
   - **Name:** `pomoechka-backend`
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** **Free**
6. Нажмите **"Create Web Service"**

## Шаг 2: Ожидание деплоя

Дождитесь завершения деплоя (2-3 минуты). Вы увидите:
- ✅ Status: Live
- 🔗 URL: `https://pomoechka-backend.onrender.com`

## Шаг 3: Обновление конфигурации

После получения URL:

1. Откройте `js/config.js`
2. Замените `null` на ваш Render URL:
   ```javascript
   API_URL: window.location.hostname === 'localhost' 
       ? 'http://localhost:3000' 
       : 'https://pomoechka-backend.onrender.com', // ← Ваш URL
   ```

3. Закоммитьте и запушьте:
   ```bash
   git add js/config.js
   git commit -m "Configure Render backend URL"
   git push origin main
   ```

## Шаг 4: Проверка

1. Откройте ваш Netlify URL
2. Откройте консоль браузера (F12)
3. Проверьте, что запросы идут на Render URL
4. Проверьте админ-панель

## ⚠️ Важно про Render Free

- Сервис "засыпает" после 15 минут бездействия
- Первый запрос после пробуждения может занять 30-60 секунд
- Это нормально для MVP и не критично

## 🔄 Альтернативы (если нужен сервис без "сна")

- **Fly.io** - бесплатно, не засыпает
- **Cyclic.sh** - бесплатно, не засыпает

Подробнее: [FREE_HOSTING_ALTERNATIVES.md](FREE_HOSTING_ALTERNATIVES.md)

