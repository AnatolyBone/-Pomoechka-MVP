# 🆓 Бесплатные альтернативы для хостинга бэкенда

Если Railway закончил пробный период, вот бесплатные альтернативы:

## 🥇 Вариант 1: Render (рекомендуется)

### Преимущества:
- ✅ **Бесплатный тариф** навсегда
- ✅ Автоматический деплой из GitHub
- ✅ PostgreSQL бесплатно (если понадобится)
- ✅ Простой интерфейс

### Деплой:

1. Зайдите на [render.com](https://render.com)
2. Войдите через **GitHub**
3. Нажмите **"New +"** → **"Web Service"**
4. Подключите репозиторий: `AnatolyBone/-Pomoechka-MVP`
5. Настройки:
   - **Name:** `pomoechka-backend`
   - **Environment:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
   - **Plan:** **Free**
6. Нажмите **"Create Web Service"**
7. Получите URL: `https://pomoechka-backend.onrender.com`

**Важно:** На бесплатном тарифе сервис "засыпает" после 15 минут бездействия. Первый запрос после пробуждения может занять 30-60 секунд.

---

## 🥈 Вариант 2: Fly.io

### Преимущества:
- ✅ **Бесплатный тариф** (3 shared-cpu VMs)
- ✅ Не засыпает
- ✅ Быстрый

### Деплой:

1. Установите Fly CLI:
   ```bash
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. Войдите:
   ```bash
   fly auth login
   ```

3. Создайте приложение:
   ```bash
   fly launch
   ```
   Следуйте инструкциям.

4. Задеплойте:
   ```bash
   fly deploy
   ```

---

## 🥉 Вариант 3: Cyclic.sh

### Преимущества:
- ✅ **Бесплатный тариф**
- ✅ Автоматический деплой из GitHub
- ✅ Не засыпает

### Деплой:

1. Зайдите на [cyclic.sh](https://cyclic.sh)
2. Войдите через **GitHub**
3. Нажмите **"Deploy Now"**
4. Выберите репозиторий: `AnatolyBone/-Pomoechka-MVP`
5. Cyclic автоматически определит Node.js
6. Получите URL: `https://pomoechka-backend.cyclic.app`

---

## 🔄 Миграция с Railway на Render

### Шаг 1: Создайте сервис на Render

Следуйте инструкциям выше для Render.

### Шаг 2: Обновите конфигурацию

После получения URL от Render:

1. Обновите `js/config.js`:
   ```javascript
   API_URL: 'https://pomoechka-backend.onrender.com', // ← Новый URL
   ```

2. Обновите CORS в `server.js` (если нужно):
   ```javascript
   const allowedOrigins = [
       'http://localhost:8000',
       'https://pomoechka-mvp-xyz123.netlify.app', // ← Ваш Netlify URL
   ];
   ```

3. Закоммитьте и запушьте:
   ```bash
   git add js/config.js
   git commit -m "Switch to Render hosting"
   git push origin main
   ```

### Шаг 3: Удалите Railway сервис (опционально)

1. Зайдите на Railway
2. Откройте ваш проект
3. Settings → Delete Project

---

## 💡 Рекомендация

**Для начала используйте Render:**
- Проще всего настроить
- Бесплатно навсегда
- Автоматический деплой из GitHub
- Единственный минус: "засыпает" после 15 минут (но это нормально для MVP)

**Если нужен сервис, который не засыпает:**
- Используйте Fly.io или Cyclic.sh

---

## 📝 Чеклист миграции

- [ ] Создан сервис на Render/Fly.io/Cyclic
- [ ] Получен URL бэкенда
- [ ] Обновлен `js/config.js` с новым URL
- [ ] Обновлен CORS в `server.js` (если нужно)
- [ ] Изменения закоммичены и запушены
- [ ] Проверена работа приложения
- [ ] Удален старый сервис на Railway (опционально)

---

## 🆘 Если что-то не работает

1. **Проверьте логи** на Render/Fly.io/Cyclic
2. **Убедитесь, что URL правильный** в `js/config.js`
3. **Проверьте CORS** - добавьте ваш Netlify URL в `allowedOrigins`
4. **Подождите 1-2 минуты** после первого деплоя (если Render - может быть задержка при пробуждении)

---

**Все эти сервисы бесплатны и подходят для MVP!** 🎉

