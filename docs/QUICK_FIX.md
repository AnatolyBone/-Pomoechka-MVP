# ⚡ Быстрое исправление - подключение к бэкенду

## Проблема

Приложение работает как презентация (демо данные), а не как полноценное приложение.

## ✅ Быстрое решение (уже применено)

Я временно указал URL бэкенда напрямую в `js/config.js`:

```javascript
API_URL: 'https://pomoechka-mvp.onrender.com'
```

## 📝 Что нужно сделать СЕЙЧАС

1. **Закоммитьте и запушьте изменения:**
   ```bash
   git add js/config.js
   git commit -m "Connect to backend"
   git push origin main
   ```

2. **Дождитесь деплоя на Netlify** (1-2 минуты)

3. **Проверьте работу:**
   - Откройте ваш Netlify URL
   - Откройте консоль (F12)
   - Должно быть: `✅ Backend connected`
   - Попробуйте создать объявление - оно должно сохраниться в базе

## 🎯 Что изменится

После подключения к бэкенду:

- ✅ **Реальные данные** - все сохраняется в SQLite на Render
- ✅ **Общая база** - все пользователи видят одни объявления
- ✅ **Система админов** - работает через базу данных
- ✅ **Нет демо** - все реальное

## 🔄 После проверки (опционально)

Если все работает, можно вернуться к переменным окружения:

1. В `js/config.js` верните `'{{API_URL}}'`
2. На Netlify установите переменную `API_URL = https://pomoechka-mvp.onrender.com`
3. Передеплойте

Но для MVP можно оставить как есть - это тоже нормально.

---

**Сейчас закоммитьте и запушьте - приложение заработает!** 🚀

