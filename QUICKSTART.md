# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
npm install
```

## Шаг 2: Запуск бэкенда

```bash
npm start
```

Или в режиме разработки (с автоперезагрузкой):

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

## Шаг 3: Проверка работы

Откройте в браузере:
- `http://localhost:3000/health` - должен вернуть `{"status":"ok"}`

## Шаг 4: Настройка фронтенда

Убедитесь, что в `js/config.js` указан правильный URL бэкенда:

```javascript
API_URL: 'http://localhost:3000'
```

## Шаг 5: Первый запуск админ-панели

1. Откройте `admin.html` в Telegram Mini App или в браузере (localhost)
2. При первом запуске появится предложение создать создателя
3. Нажмите "Подтвердить и войти"
4. Ваш Telegram ID будет сохранен как создатель

## Шаг 6: Использование

### Основное приложение
- Откройте `index.html`
- Все данные теперь сохраняются в базе данных

### Админ-панель
- Откройте `admin.html`
- Только создатель может добавлять других администраторов
- Администраторы могут модереровать контент и изменять настройки

## 📝 Важные замечания

1. **База данных** создается автоматически в папке `data/pomoechka.db`
2. **Создатель** - первый пользователь, который инициализировал систему. Только он может управлять администраторами.
3. **Администраторы** - пользователи, назначенные создателем. Могут модереровать контент, но не могут управлять другими админами.

## 🔧 Решение проблем

### Бэкенд не запускается
- Проверьте, что порт 3000 свободен
- Убедитесь, что установлены все зависимости: `npm install`

### Админ-панель не работает
- Проверьте, что бэкенд запущен
- Убедитесь, что в `js/config.js` указан правильный `API_URL`
- Проверьте консоль браузера на наличие ошибок

### База данных не создается
- Убедитесь, что у приложения есть права на запись в папку `data/`
- Проверьте логи сервера на наличие ошибок

