# 🧪 Тест подключения к бэкенду

## ✅ Что видно в консоли

```
📡 Backend mode: https://pomoechka-mvp.onrender.com
```

URL правильный! Но нет `✅ Backend connected`.

## 🔍 Диагностика

### Шаг 1: Проверьте бэкенд напрямую

Откройте в новой вкладке браузера:
```
https://pomoechka-mvp.onrender.com/health
```

**Ожидаемый результат:**
```json
{
  "status": "ok",
  "timestamp": "2026-01-04T..."
}
```

**Если не открывается или долго грузит:**
- Бэкенд "спит" (Render Free засыпает после 15 минут бездействия)
- Подождите 30-60 секунд - первый запрос после пробуждения медленный
- Попробуйте еще раз

### Шаг 2: Проверьте консоль браузера на ошибки

Откройте консоль (F12) → вкладка Console и ищите:

**Если видите:**
- ❌ `CORS policy: No 'Access-Control-Allow-Origin'` - проблема с CORS
- ❌ `Failed to fetch` - бэкенд недоступен
- ❌ `Network error` - проблема с сетью
- ❌ `❌ Backend error: ...` - детали ошибки

**Если НЕ видите ошибок:**
- Возможно, запрос просто долго выполняется
- Подождите еще немного

### Шаг 3: Проверьте вкладку Network

1. Откройте DevTools (F12)
2. Вкладка **Network**
3. Обновите страницу (F5)
4. Найдите запрос к `/health`
5. Посмотрите статус:
   - ✅ `200 OK` - бэкенд работает
   - ❌ `CORS error` - нужно добавить URL в CORS
   - ❌ `Failed` - бэкенд недоступен
   - ⏳ `Pending` - бэкенд "спит", ждите

## 🔧 Решение проблем

### Проблема 1: Бэкенд "спит"

**Симптомы:**
- Первый запрос занимает 30-60 секунд
- Потом работает нормально

**Решение:**
- Это нормально для Render Free
- Можно использовать UptimeRobot для "пробуждения" каждые 5 минут

### Проблема 2: CORS ошибка

**Симптомы:**
- В консоли: `CORS policy: No 'Access-Control-Allow-Origin'`
- В Network: статус `CORS error`

**Решение:**
Я уже обновил `server.js` - теперь CORS разрешает все origin в продакшне. Нужно передеплоить бэкенд:

```bash
git add server.js
git commit -m "Fix CORS for production"
git push origin main
```

Render автоматически передеплоит (1-2 минуты).

### Проблема 3: Бэкенд не отвечает

**Симптомы:**
- Запрос к `/health` не открывается
- В Network: `Failed` или `Timeout`

**Решение:**
1. Проверьте логи на Render
2. Убедитесь, что сервис запущен
3. Попробуйте перезапустить сервис на Render

## 🎯 После исправления

Когда все заработает, в консоли будет:
```
📡 Backend mode: https://pomoechka-mvp.onrender.com
✅ Backend connected
```

И приложение начнет работать с реальной базой данных!

---

**Сначала проверьте бэкенд напрямую в браузере!** 🔍

