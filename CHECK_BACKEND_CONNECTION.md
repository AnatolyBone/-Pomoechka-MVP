# 🔍 Проверка подключения к бэкенду

## ✅ Хорошие новости

В консоли видно:
```
📡 Backend mode: https://pomoechka-mvp.onrender.com
```

URL правильный! Но нет сообщения `✅ Backend connected`.

## 🔴 Проблема

Запрос к `/health` не проходит. Возможные причины:

1. **CORS ошибка** - бэкенд блокирует запросы с Netlify
2. **Бэкенд "спит"** - Render Free засыпает после 15 минут
3. **Ошибка сети** - временная проблема

## ✅ Решение

### Шаг 1: Проверьте бэкенд напрямую

Откройте в браузере:
```
https://pomoechka-mvp.onrender.com/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "timestamp": "..."
}
```

**Если не открывается:**
- Бэкенд "спит" (Render Free)
- Подождите 30-60 секунд и попробуйте снова
- Первый запрос после пробуждения может быть медленным

### Шаг 2: Добавьте Netlify URL в CORS

Если бэкенд работает, но есть ошибки CORS:

1. Узнайте ваш Netlify URL (например: `https://pomoechka-xyz123.netlify.app`)
2. Откройте `server.js`
3. Найдите массив `allowedOrigins`
4. Добавьте ваш Netlify URL:
   ```javascript
   const allowedOrigins = [
       'http://localhost:8000',
       'http://localhost:3000',
       'https://your-netlify-url.netlify.app', // ← Добавьте ваш Netlify URL
   ];
   ```
5. Закоммитьте и запушьте:
   ```bash
   git add server.js
   git commit -m "Add Netlify URL to CORS"
   git push origin main
   ```
6. Render автоматически передеплоит (1-2 минуты)

### Шаг 3: Проверьте консоль браузера на ошибки

Откройте консоль (F12) → вкладка Console и ищите:
- ❌ `CORS policy` - значит нужно добавить URL в CORS
- ❌ `Failed to fetch` - значит бэкенд недоступен
- ❌ `Network error` - проблема с сетью

## 🎯 После исправления

Когда все заработает, в консоли будет:
```
📡 Backend mode: https://pomoechka-mvp.onrender.com
✅ Backend connected
```

И приложение начнет работать с реальной базой данных!

---

**Сначала проверьте бэкенд напрямую, потом добавьте Netlify URL в CORS!** 🔧

